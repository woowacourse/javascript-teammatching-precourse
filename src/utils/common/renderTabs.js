import $ from './selector.js';
import { templates } from '../../constants/templates/tabs.js';

export const renderTabs = () => {
  // $('#app').insertAdjacentHTML('afterend', templates.tabs);
  $('#app').innerHTML = '<div>뭔데?</div>';
};
